# Node modules
node_modules/

# Temporary files
*.tmp
*.log
sample_data.txt

# Keep the master lists for direct download, but ignore the raw downloads that
# download-sources.sh refetches on every run. The filenames here used to be
# stale, which left ieee_mal.csv and friends untracked-but-not-ignored.
sources/

# Keep all output files - these are what users want!
# output/ folder will be committed
